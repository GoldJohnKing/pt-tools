schema: spec-driven

# Project context
context: |
  Tech stack: Go 1.25+, Vue 3, Vite 7, Bun (package manager)
  Architecture: Go backend with embedded Vue 3 SPA frontend
  Frontend: web/frontend/ (Vue 3 + TypeScript + Element Plus)
  Backend: Go with embedded static files via go:embed
  Build: Refer to build.bat
  Domain: PT (Private Tracker) automation tool
  语言：中文（简体）
  所有产出物必须用简体中文撰写。

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
